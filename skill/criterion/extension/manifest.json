{
  "manifest_version": 3,
  "name": "Criterion Design Detector",
  "version": "1.0.0",
  "description": "Runs the criterion skill's deterministic anti-pattern detector against the page you're viewing. No LLM, no API key, no data leaves the browser.",
  "permissions": ["activeTab", "scripting"],
  "action": {
    "default_popup": "popup.html",
    "default_title": "Criterion — scan this page"
  }
}
