# PRODUCT.md — example shape

The canonical product-context file written by `init`. Copy this shape; keep it tight.

```markdown
# Product
what it does: <one sentence>
users: <who, and how often — e.g. "support engineers, 40h/week">
frequency: <daily driver vs occasional>

# Most frequent jobs (top 3)
1. <job one — e.g. "see open tickets and their age/owner/severity">
2. <job two>
3. <job three>

# Registers per surface
/tickets: Command · /settings: Configure · /reports: Record · /pricing: Convince

# Data reality
entities: <the real nouns and their statuses — e.g. "tickets: open/assigned/closed">
extremes: <longest string, most rows, smallest screen, empty dataset>

# Constraints
framework: <stack> · browsers: <targets> · i18n: <locales or none>
a11y: <WCAG AA / internal-only / etc.>
```
